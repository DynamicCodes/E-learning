import LoginPage from "../components/LoginPage";
import styles from "./LP.module.css";

const LP = () => {
  return (
    <div className={styles.lp}>
      <LoginPage />
    </div>
  );
};

export default LP;
